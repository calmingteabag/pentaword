var word = 'panda';
var arr = '';

function getKeypress(current) {
    // var current_key = document.getElementById('keybutton').innerHTML;
    var c = document.getElementsByClassName(current);
    console.log(c);
    // add current key to key position in html
};

